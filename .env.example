# Copy this file to .env and fill in values.

# Django
SECRET_KEY=your-secret-key-here
DEBUG=True
ALLOWED_HOSTS=localhost 127.0.0.1

# PostgreSQL (only needed in production; leave blank to use SQLite in dev)
DB_NAME=stylnyashki
DB_USER=postgres
DB_PASSWORD=your-db-password
DB_HOST=localhost
DB_PORT=5432
